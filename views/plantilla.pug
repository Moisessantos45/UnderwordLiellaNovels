doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title Panel de admnistracion
        link(rel='stylesheet' href='css/login.css')
        link(rel='icon' href='/img/Marca.ico')
    body
        h1  Esta es la pagina de administracion
        if volumene
            h1= volumene.usuario
            p= volumene.contraseña
        else
            p= "No se encontró ningún usuario"
